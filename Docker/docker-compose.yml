version: '3.8'

services:
  api:
    build: .
    container_name: si3ln-api
    ports:
      - "8000:8000"
    volumes:
      - ./Base_de_donnée/db.sqlite3:/app/db.sqlite3
      - ./:/app
    environment:
      - DJANGO_DEBUG=True
      - DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
    